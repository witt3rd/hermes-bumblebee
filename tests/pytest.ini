[pytest]
# This pytest.ini lives in tests/ (not repo root) so that pytest's rootdir
# discovery stops here. If it went up to the repo root it would try to
# import the plugin __init__.py (which uses relative imports requiring the
# Hermes plugin loader to work).
addopts = --import-mode=importlib
python_files = test_*.py
